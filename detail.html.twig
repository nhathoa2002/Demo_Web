{% extends "base.html.twig" %}

{% block title %} Delivery detail {% endblock %}

{% block body %}
<div class="container col-md-6 mt-5">
   <div class="row">
      <div class="col">
         <img src="{{asset('images/delivery/' ~ delivery.image)}}" width="300" height="400">  
      </div>
      <div class="col text-center mt-3">
         <h1 class="text text-success">{{ delivery.name }}</h1>
         <h2>Description: {{ delivery.description }}</h2>
         <h2>Orders: </h2>
         {# {% for order in delivery.orders %}
            <a href="{{ path('order_detail', {'id' : order.id })}}">
              <h4 class="text text-primary">{{ order.id }}</h4> 
            </a> 
         {% endfor %} #}
      </div>
   </div>
</div>
{% endblock %}